# v2.0.4
### GFSDK version 2.0.4
* startSession waits userData if called before
* added tracking for GameLoad on hybrid case and go to home behaviour
* fix removeEventListener when the back button in gameover is called(did not impact the exeperience)
* dates for gameInfo local and server are now compliant