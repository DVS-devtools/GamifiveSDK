{
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "build:web": "browserify src/builders/build_web.js -o dist/gfsdk_web.js -s GamifiveSDK",
    "build:debug": "browserify src/builders/build_debug.js -o dist/gfsdk_debug.js -s GamifiveSDK",
    "build:hybrid": "browserify src/builders/build_hybrid.js -o dist/gfsdk_hybrid.js -s GamifiveSDK",
    "build:offline": "browserify src/builders/index_offline.js -o dist/gfsdk_offline.js -s GamifiveSDK",
    "build:offlineutils": "browserify src/builders/index_offline_utils.js -o dist/gfsdk_offline_utils.js -s GamifiveSDK",
    "test": "exit 0"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "jasmine-core": "^2.2.0",
    "jsdoc": "3.2.2",
    "karma": "^0.12.31",
    "karma-coverage": "^0.2.7",
    "karma-jasmine": "^0.1.0",
    "karma-phantomjs-launcher": "^0.1.4",
    "phantomjs": "^1.9.16"
  },
  "dependencies": {
   
  }
}
